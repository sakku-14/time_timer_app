import 'package:flutter/material.dart';

class TimerOption extends StatelessWidget {
  const TimerOption({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('timer_option');
  }
}
